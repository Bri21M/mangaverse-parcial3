# Núcleo Flask
flask>=3.0
flask-sqlalchemy>=3.1
flask-login>=0.6
werkzeug>=3.0

# Driver MySQL (instalar cuando migres a nube)
pymysql>=1.1
cryptography>=42.0   # requerido por pymysql para conexiones seguras
